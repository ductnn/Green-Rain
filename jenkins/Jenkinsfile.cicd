node {
    checkout()
    deploy()
}

def checkout() {
    stage('Clone') {
        checkout scm
    }
}

def deploy() {
    stage('Deploy') {
        dir (".") {
            sh 'ls -la'
            sh '/var/jenkins_home/workspace/kube/kubectl apply -f deployment.yaml'
        }
    }
}
