apiVersion: v1
kind: Service
metadata:
  name: {{ include "green-rain.fullname" . }}
  labels:
    {{- include "green-rain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  {{- with .Values.service.ports }}
  ports:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    {{- include "green-rain.selectorLabels" . | nindent 4 }}
